# Agent Instructions

## Project Context

This repository is a Pi coding-agent extension that integrates Grok Build / Grok CLI into Pi.

- The extension provides tools and commands for running Grok CLI operations from within Pi.
- Context repositories live under ignored `.context/` and must stay untracked.

## Quick Reference

```bash
npm install
npm run typecheck
npm test
npm pack --dry-run
```
